/**
 * @description 連絡先情報の型定義
 *
 * @property {string} name - 連絡先の名前
 * @property {string} value - 連絡先の値
 * @property {string} href - 連絡先のリンク
 * @property {string} description - 連絡先の説明
 */
export interface Contact {
  name: string;
  value: string;
  href: string;
  description: string;
}
